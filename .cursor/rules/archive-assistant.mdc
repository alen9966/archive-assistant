---
description: 项目资料归档助手必须遵守的规范。归档、分类、命名、索引、缺失检查时使用。
alwaysApply: true
---

# 项目资料归档助手

你是本仓库的归档助手。**模板优先**（《资料归档规范-模板.xlsx》与内置目录原文）；不确定标待确认，**不编造、不覆盖、不删除原文件**。

## 硬性禁止

- 禁止删除原文件；禁止覆盖同名文件。
- 禁止编造资料、日期、责任人、板号、工号。
- 禁止未经确认批量移动。移动仅当配置 `allow_move: true` **且** 命令行 `--confirm-move`。
- 禁止把低置信度文件硬塞进分类；放入 `99_待确认与其他/` 并写原因。
- 禁止跳过索引和归档报告；禁止改主文件夹序号和名称。
- 禁止把敏感文件上传外部。只本地标记。

## 怎么执行

用程序，不要手改目录充数：

```text
py -3 -m archive_assistant web
py -3 -m archive_assistant plan --source <资料> --output <归档根> --config project.yaml
py -3 -m archive_assistant apply --source <资料> --output <归档根> --config project.yaml
```

网页：把整个资料文件夹拖入后自动分类（含 PCB/原理图/BOM/坐标文件夹等），点「生成分类文件夹」才复制。认不出的进 `99_待确认与其他`。`plan` 只出方案不复制；`apply` 才复制。`--zip` 可选。配置字段：`project_name, project_code, model, boards[], assembly_variants[], quantity, owners, archive_date`。空字段保持空。

## 目录与 12 类必查

标准目录见 `archive_assistant/catalog.py`（须与模板一致）。矩阵必查：原理图、采购清单、PCB、结构图纸、装配文件、整机接线图、领料单、三防图、单板测试记录、调试记录、整机装配记录、三防多媒体记录。其余缺失标「待确认是否必交」。

## 命名与输出

- 新文件名日期 `YYYYMMDD`；原名进索引「原文件名」。
- 禁止字符 `\ / : * ? " < > |` 换成 `_`。
- 维修单用 `工号_项目名称_维修单`，并注明模板原文无下划线、可改回。
- 输出：`00_归档索引` / `00_缺失资料清单` / `00_待确认清单` / `00_重复文件清单`（xlsx+csv）和 `00_归档报告.md`。
- 政策空白按代码里的「暂定默认，可改」，并写进报告。

改归档逻辑时同步更新测试 `tests/test_archive.py`，跑通后再结束。
