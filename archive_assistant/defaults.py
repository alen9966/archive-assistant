"""暂定默认政策（用户未确认，可改）。程序与报告均须写明本说明。"""

POLICY_NOTE = "暂定默认，可改"

TODAY_DEFAULT = "20260917"  # 仅作示例；实际运行日以系统日期为准

DEFAULT_RULES = [
    "日期：新文件名统一 YYYYMMDD；原文件名写入索引「原文件名」；历史 YYMMDD 只保留在原名。",
    "编号：从文件名或配置提取，不编造。配置字段：project_name, project_code, model, boards[], assembly_variants[], quantity, owners, archive_date。板号版本如 ZC7.820.0001-V6.1 保持原样。",
    "必交：对「板号-装配变量-套数矩阵」检查 12 类（原理图、采购清单、PCB、结构图纸、装配文件、整机接线图、领料单、三防图、单板测试记录、调试记录、整机装配记录、三防多媒体记录）。其余模板项缺失时状态为「待确认是否必交」，不编造成必缺。",
    "多人负责人：索引记录全部角色；实际确认人写入备注；没有则待确认。",
    "默认只复制，不移动、不删除。移动必须配置 allow_move=true 且命令行 --confirm-move，默认关闭。禁止覆盖。",
    "输出：xlsx（openpyxl）+ 同名 csv（utf-8-sig）兜底；00_归档报告.md；zip 仅 --zip，默认不打。",
    "软件 BOOT_(用户)_产品/名称_(功能)_日期.bin：从原文件名提取用户/功能；提取不到标待确认，不编造。固件文件夹：工号_文件类型_归档日期。",
    "结构图纸命名：工号_型号或产品名称_日期；优先型号，没有用项目名称；模板里的 / 在实际文件名改为 _。禁止字符 \\ / : * ? \" < > | 全部替换为 _。",
    "发货记录、多媒体记录：无模板专名时用 项目工号_项目名称_文件类型_日期。多媒体按阶段分子目录：电装完 / 三防前 / 三防后 / 发货前 / 包装箱（能识别则放入，不能识别则待确认）。",
    "维修单文件名按 工号_项目名称_维修单 实现；模板原文无下划线，已按可读性补下划线，可改回。",
    "分类：低置信度不硬塞，入 99_待确认与其他。整夹丢入时优先按源文件夹名识别（PCB/原理图/BOM/坐标文件夹等）。关键词表中单独的 PDF 过宽，已忽略，避免误分类。",
    "敏感文件只标记不外传；不上传外部服务。",
    "plan 默认不复制；--apply 才复制。",
]


def policy_markdown() -> str:
    lines = ["本次使用的暂定默认规则（用户未确认，可改）：", ""]
    for i, item in enumerate(DEFAULT_RULES, 1):
        lines.append(f"{i}. {item}")
    return "\n".join(lines)
